" finding files
" - tab to :find a partial match
" - use * to make it fuzzy
" - :b autocomplete a buffer

" search recursively with tab completion
set path+=**

" display all matching files when we tab
set wildmenu
